{
    "$schema": "http://json-schema.org/draft-04/schema",
    "description": "This is how you should define your message send to the hello endpoint",
    "properties": {
        "greeting": {
            "type": "string",
            "description": "The message you will use to greet the hello endpoint"
        },
        "sendTime": {
            "type": "string",
            "format": "date-time",
            "description": "The datetime at which your greeting was sent"
        }
    },
    "required":[
        "greeting"
    ],
    "additionalProperties": false
}